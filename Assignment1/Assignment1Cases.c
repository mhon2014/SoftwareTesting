#include <stdio.h>
#include <stdlib.h> 
#include <sys/types.h>
#include <unistd.h> 
#include <string.h>

#define MAX_LIMIT 256
int main(){
    char *line = NULL; //used to read entire line
    char *token = NULL; //tokens for seperating the words
    char *linetoken = NULL;
    size_t len_of_line = 0; //to save the length of the line for getline
    int dssize = 0;

    scanf("%d", &dssize);
    printf("%d",dssize);

    // while(getline(&line, &len_of_line, stdin) >= 0) {
        
    // }
        
    exit(0);
}

void onetoone(){

}

void onto(){
    
}

void reflexive(){
    
}

void symmetric(){
    
}

void transitive(){

}

void function(){

}
